# data/

En el Proyecto Final **no hay dataset local**: los datos llegan **por lotes** desde
la API externa (`cristiandiaz13/mlops-puj:data-api-pf-v1`) y se almacenan en la base
`RAW DATA` (tabla `raw_properties`).

Esta carpeta queda solo para muestras o fixtures de prueba locales si se necesitan.
